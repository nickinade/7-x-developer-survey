package com.scandit.datacapture.idcapturesurvey.scan

import androidx.lifecycle.ViewModel

class ScanViewModel: ViewModel() {

    init {
        setUpIdCapture()
    }

    private fun setUpIdCapture() {
        // Initialize Scandit SDK and ID Capture
    }
}